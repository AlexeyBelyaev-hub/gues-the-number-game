<thlogic>
    <attr sel = "#headId"
            th:replace="~{fragments/head :: headFragment}"/>
    <attr sel = "#main-message"
          th:text="${__${T(academy.learnprogramming.util.Attributes).MAIN_MESSAGE}__}"/>
    <attr sel = "#result-message"
          th:text="${__${T(academy.learnprogramming.util.Attributes).RESULT_MESSAGE}__}"/>

    <attr sel = "#game-form"
          th:action="@{__${T(academy.learnprogramming.util.Mappings).PLAY}__}"/>
    <attr sel = "#footerId"
          th:replace="~{fragments/footer::footerFragment}"/>

</thlogic>